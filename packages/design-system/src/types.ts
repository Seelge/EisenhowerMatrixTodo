/**
 * Public types for the design system package.
 * Currently re-exports the token typings; expand as primitives land.
 */

export type { ColorToken, GlowToken, Quadrant, RadiusToken, SpaceToken, Tokens } from './tokens.ts';
