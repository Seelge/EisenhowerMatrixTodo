node_modules
dist
*.tsbuildinfo
.DS_Store
*.log
.vite
playwright-report
test-results
coverage
.turbo
# Lighthouse CLI on WSL2 sometimes drops a Chromium profile dir with a
# literal `C:\...` name into the cwd. Ignore so it never gets staged.
C:\\*

# Local tool config
.claude/
.vscode/
.idea/

# Secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key
secrets.*
